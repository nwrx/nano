import { create } from '@storybook/theming'

export const theme = create({
  base: 'dark',

  // // Typography
  // fontBase: '"IBM Plex Sans", sans-serif',
  // fontCode: '"Fira Code", monospace',
  // gridCellSize: 64,

  // // Brand colors
  // colorPrimary: COLORS.primary[500],
  // colorSecondary: COLORS.primary[400],

  // // UI
  // appBg: COLORS.primary[900],
  // appContentBg: `${COLORS.primary[50]}20`,
  // appPreviewBg: COLORS.primary[50],
  // appBorderColor: `${COLORS.light[500]}40`,
  // appBorderRadius: 8,

  // booleanBg: COLORS.primary[900],
  // booleanSelectedBg: COLORS.primary[500],

  // buttonBg: COLORS.primary[400],
  // buttonBorder: `${COLORS.light[500]}40`,

  // // Text colors
  // textColor: COLORS.primary[900],
  // textInverseColor: COLORS.primary[800],
  // textMutedColor: `${COLORS.primary[800]}80`,

  // // Toolbar default and active colors
  // barTextColor: `${COLORS.primary[50]}40`,
  // barSelectedColor: COLORS.primary[50],
  // barHoverColor: COLORS.primary[50],
  // barBg: COLORS.primary[900],

  // // Form colors
  // inputBg: COLORS.primary[900],
  // inputBorder: `${COLORS.light[500]}40`,
  // inputTextColor: COLORS.primary[50],
  // inputBorderRadius: 8,
})
