{{- if .Values.database.enabled -}}
{{- if .Values.database.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "nano.database.serviceAccountName" . }}
  labels:
    {{- include "nano.labels" . | nindent 4 }}
  {{- with .Values.database.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.database.serviceAccount.automountServiceAccountToken | default false }}
{{- end }}
{{- end }}
